{
  "requiredFields": ["serviceName", "riskLevel"],
  "maxRiskLevel": 2,
  "rules": [
    {
      "id": "required:serviceName",
      "description": "Service name must be present"
    },
    {
      "id": "required:riskLevel",
      "description": "Risk level must be present and within bounds"
    }
  ]
}
